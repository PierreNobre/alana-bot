specVersion: "v1beta"
serviceId: "crewai"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "crewai-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
